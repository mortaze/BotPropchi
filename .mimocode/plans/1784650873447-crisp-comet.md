# Plan: Feature Flag / Service Manager — Fill Existing Gaps

## Current State

The feature toggle system **already exists**. DB model, API, bot middleware, menu filtering, admin UI toggles, and handler-level checks are all in place. The user's request matches what's already built.

## What's Missing

### Gap 1: Admin sidebar doesn't filter by features
`admin/src/components/layout/Sidebar.tsx` — static `menuItems` array, no feature filtering.

### Gap 2: `featureForCallback()` incomplete
`src/bot/service-toggle.ts:14` — only maps `lottery`, `ticket_system`, `referrals`. Missing callbacks for `posts`, `forex_news`, `leaderboard`.

### Gap 3: Bot admin keyboard not feature-aware
`src/bot/keyboards/index.ts:86` — `buildBotAdminPanelKeyboard()` always shows all buttons.

## Files to Modify

### 1. `src/bot/service-toggle.ts`
Extend `featureForCallback()` to cover all callback prefixes:
- `post:` → `posts`
- `news:` → `forex_news`
- `leaderboard:` / `lb:` → `leaderboard`

### 2. `admin/src/components/layout/Sidebar.tsx`
- Add `featureKey?: string` to `MenuItem` interface
- Add `featureKey` to relevant sidebar items (lotteries→lottery, referrals→referrals, ticket-categories→ticket_system, scoring→points, required-channels→force_join, automation→auto_replies, analytics→reports)
- Use `useQuery` to fetch features from `settingsApi.getFeatures()`
- Filter `menuItems` before rendering: skip items whose `featureKey` is disabled

### 3. `src/bot/keyboards/index.ts`
Add optional `features?: Record<string, boolean>` param to `buildBotAdminPanelKeyboard()`:
- Hide `🎫 تیکت‌ها` when `ticket_system` is off
- Hide `📰 اخبار` when `forex_news` is off
- Hide `🤖 اتوماسیون` when `auto_replies` is off

### 4. Callers of `buildBotAdminPanelKeyboard()` (6 files, ~12 call sites)
Pass features map from `settingsService.getFeatureMap()`:
- `src/bot/handlers/index.ts` (lines 316, 427, 731)
- `src/bot/handlers/post-handlers.ts` (line 133)
- `src/bot/handlers/news-admin.handlers.ts` (lines 183, 395, 446)
- `src/bot/handlers/scheduled-message.handlers.ts` (line 110)
- `src/bot/handlers/ticket-admin.handler.ts` (line 260)
- `src/index.ts` (line 133)

## Verification

1. `cd admin && npm run lint` — admin lint passes
2. `npm run build` — root TypeScript compiles
3. `npm run test` — existing tests pass
