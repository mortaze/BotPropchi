# Plan: Posts Section UI Simplification

## Goal
Unify the posts management page so that clicking "📝 پست‌ها" from the admin panel shows one page with:
1. "➕ ایجاد پست" button at the top
2. List of all posts below (existing structure preserved)
3. "↩️ بازگشت به پنل ادمین" at the bottom
4. Remove "📊 آمار پست" section entirely
5. Remove "🔙 بازگشت به منوی پست‌ها" button entirely

## Files to Modify

### 1. `src/bot/keyboards/post-keyboards.ts`

**`buildPostListFromMenuLayout()`** (line 120-152):
- Add `['➕ ایجاد پست']` as the FIRST row (before post list)
- Remove `rows.push(['🔙 بازگشت به منوی پست‌ها'])` (line 150)
- Add `rows.push(['↩️ بازگشت به پنل ادمین'])` at the end

**`postMainMenuKeyboard()`** (line 12-18):
- Remove `['📊 آمار پست']` row
- Change `['↩️ بازگشت به پنل ادمین']` to remain as-is (already exists)

### 2. `src/bot/handlers/post-handlers.ts`

**`➕ ایجاد post` handler** (line 585-593):
- After successful post creation (line 674-679), instead of showing `postNewPostManagerReplyKeyboard()`, call `showPostListFromLayout(ctx)` to re-render the unified page

**`🔙 بازگشت به منوی پست` text handler** (line 454-457):
- Remove this handler entirely (no longer needed)

**`↩️ بازگشت به پنل ادمین` handler** (line 2563-2567):
- Keep as-is (already works correctly)

**`post:menu` action handler** (line 3490-3497):
- Modify to show unified page instead of `postMainMenuKeyboard()`:
  - Call `showPostListFromLayout(ctx)` instead of `ctx.reply('📝 سامانه مدیریت پست‌ها', postMainMenuKeyboard())`

### 3. `src/bot/handlers/index.ts`

**knownTexts array** (line 608-618):
- Remove `'📊 آمار پست'` and `'📊 آمار کلی'` from the list (buttons no longer exist)

## Detailed Changes

### Change 1: `buildPostListFromMenuLayout` — Add create button at top, change back button

```typescript
// BEFORE (line 120-152):
export const buildPostListFromMenuLayout = (layout: any[][], drafts?: any[]) => {
  const rows: string[][] = layout
    .filter(row => Array.isArray(row))
    .map(row =>
      row
        .filter((btn: any) => btn && btn.ref && btn.ref.startsWith('post:'))
        .map((btn: any) => {
          const text = btn.text || btn.label || btn.title || btn.ref || 'بدون عنوان';
          return sanitizeTelegramText(text, 128);
        })
    )
    .filter((row: string[]) => row.length > 0);
  if (drafts && drafts.length > 0) {
    // ... draft handling ...
  }
  rows.push(['🚀 پیام Start']);
  rows.push(['📩 پیام ناشناس']);
  rows.push(['🔙 بازگشت به منوی پست‌ها']);  // REMOVE THIS
  return Markup.keyboard(rows).resize().persistent();
};

// AFTER:
export const buildPostListFromMenuLayout = (layout: any[][], drafts?: any[]) => {
  const rows: string[][] = [];
  rows.push(['➕ ایجاد پست']);  // ADD: Create post button at top
  // ... existing layout processing ...
  rows.push(['🚀 پیام Start']);
  rows.push(['📩 پیام ناشناس']);
  rows.push(['↩️ بازگشت به پنل ادمین']);  // CHANGE: back to admin panel
  return Markup.keyboard(rows).resize().persistent();
};
```

### Change 2: Post creation — Show post list after creating

```typescript
// BEFORE (line 674-679):
const post = await postService.create({ title, slug, createdBy: BigInt(ctx.from.id) });
cache.setPermanent(editorKey(ctx.from.id, 'active'), post.id);
cache.setPermanent(editorKey(ctx.from.id, 'mode'), 'new_post_manager');
await ctx.reply(`✅ پست ساخته شد!\n\nعنوان: ${title}\nاسلاگ: ${slug}`, {
  ...postNewPostManagerReplyKeyboard(),
});

// AFTER:
const post = await postService.create({ title, slug, createdBy: BigInt(ctx.from.id) });
await ctx.reply(`✅ پست ساخته شد!\n\nعنوان: ${title}\nاسلاگ: ${slug}`);
await showPostListFromLayout(ctx);
```

### Change 3: Remove back-to-posts-menu handler

```typescript
// REMOVE (line 454-457):
if (text === '🔙 بازگشت به منوی پست' || text === '🔙 بازگشت به منوی پست‌ها') {
  cache.del(`post_mgmt_mode:${ctx.from.id}`);
  return ctx.reply('📝 سامانه مدیریت پست‌ها', postMainMenuKeyboard());
}
```

### Change 4: `post:menu` action — Show unified page

```typescript
// BEFORE (line 3490-3497):
bot.action('post:menu', async (ctx) => {
  await ctx.answerCbQuery();
  const admin = await requirePostAdmin(ctx);
  if (!isPostAdmin(admin)) return;
  clearEditorKeyState(ctx.from.id);
  cache.del(`post_mgmt_mode:${ctx.from.id}`);
  await ctx.reply('📝 سامانه مدیریت پست‌ها', postMainMenuKeyboard());
});

// AFTER:
bot.action('post:menu', async (ctx) => {
  await ctx.answerCbQuery();
  const admin = await requirePostAdmin(ctx);
  if (!isPostAdmin(admin)) return;
  clearEditorKeyState(ctx.from.id);
  cache.setPermanent(`post_mgmt_mode:${ctx.from.id}`, true);
  await showPostListFromLayout(ctx);
});
```

### Change 5: `postMainMenuKeyboard` — Remove stats button

```typescript
// BEFORE (line 12-18):
export const postMainMenuKeyboard = () =>
  Markup.keyboard([
    ['➕ ایجاد پست'],
    ['📋 مدیریت پست‌ها'],
    ['📊 آمار پست'],           // REMOVE
    ['↩️ بازگشت به پنل ادمین'],
  ]).resize().persistent();

// AFTER:
export const postMainMenuKeyboard = () =>
  Markup.keyboard([
    ['➕ ایجاد پست'],
    ['📋 مدیریت پست‌ها'],
    ['↩️ بازگشت به پنل ادمین'],
  ]).resize().persistent();
```

### Change 6: knownTexts — Remove stats buttons

```typescript
// BEFORE (line 616):
'📊 آمار پست', '📊 آمار کلی', '🔍 بررسی سلامت',

// AFTER:
'🔍 بررسی سلامت',
```

## Verification

1. Run `npm run build` to verify TypeScript compilation
2. Run `npm run test` to verify no test regressions
3. Manual testing:
   - Click "📝 پست‌ها" from admin panel → should show create button + post list + back to admin
   - Create a post → should return to post list showing the new post
   - Click "↩️ بازگشت به پنل ادمین" → should go back to admin panel
   - "📊 آمار پست" button should no longer appear
   - "🔙 بازگشت به منوی پست‌ها" button should no longer appear
